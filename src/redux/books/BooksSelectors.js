export const getBooks = state => state.books.books;

export const getBookStore = state => state.storeBooks.storeBooks;

export const getTotalBooks = state => state.storeBooks.totalBooks;

export const getCashBooks = state => state.storeBooks.cashBooks;
